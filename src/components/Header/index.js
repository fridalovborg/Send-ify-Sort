import React from 'react';
import { HeaderContent, Title } from './styles';

const Header = () => {
    return (
        <HeaderContent>
            <Title>Sendify Price Sorting</Title>
            <p>Sort by</p>
        </HeaderContent>
    );
};
export default Header;